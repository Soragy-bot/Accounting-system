services:
  accounting-system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accounting-system
    ports:
      - "8880:8880"
    restart: unless-stopped
    networks:
      - accounting-network

networks:
  accounting-network:
    driver: bridge
