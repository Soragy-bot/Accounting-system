services:
  accounting-system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accounting-system
    ports:
      - "27015:80"
    restart: unless-stopped
    networks:
      - accounting-network

networks:
  accounting-network:
    driver: bridge

